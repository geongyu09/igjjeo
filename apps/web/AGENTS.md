<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# 문서 위치 안내

이 워크스페이스의 문서는 여기에 두지 않습니다. 모든 프로젝트 문서는 루트 `.claude/`에 모여 있습니다.

- **이 워크스페이스(apps/web)를 수정하기 전에 `web-workspace` 스킬을 먼저 사용하세요.** 구현 구조·컨벤션·작업별 필독 문서 인덱스가 담겨 있습니다. (원본: `.claude/skills/web-workspace/SKILL.md`)
- 프로젝트 전체 규칙: 루트 `CLAUDE.md` 및 `.claude/rules/`
- 도메인 명세: `igjjeo-spec` 스킬

위의 `nextjs-agent-rules` 블록은 도구가 관리하는 자동 생성 영역이므로 그대로 둡니다.
