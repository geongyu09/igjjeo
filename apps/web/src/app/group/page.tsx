import { GroupHubSection } from "@/components/feature/pages/group/GroupHubSection";

/** 방 허브(로그인 후 랜딩) — 방 선택·초대 코드 참여·새 방 만들기. */
export default function GroupPage() {
  return <GroupHubSection />;
}
