# Vercel은 install을 직접 수행하므로 의존성/빌드 산출물은 업로드하지 않는다.
# (모노레포 전체를 루트에서 업로드하되 apps/web 빌드에 불필요한 대용량 제외)
node_modules
.next
out
dist
build

# Expo/네이티브 (apps/web 빌드와 무관, 매우 큼)
ios
android
.expo
web-build

# 기타
.git
*.log
*.tsbuildinfo
.DS_Store
.claude
context
.env
.env.*
!.env.example
